{
  "directories": {
    "test": "test"
  },
  "author": "azu",
  "license": "MIT",
  "files": [
    "bin/",
    "lib/",
    "src/"
  ],
  "name": "babel-plugin-strip-function-call",
  "version": "1.0.1",
  "description": "Babel plugin strip any function call.",
  "main": "lib/babel-plugin-strip-function-call.js",
  "scripts": {
    "test": "mocha test/",
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build"
  },
  "keywords": [
    "babel",
    "plugin",
    "optimize",
    "production"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/azu/babel-plugin-strip-function-call.git"
  },
  "bugs": {
    "url": "https://github.com/azu/babel-plugin-strip-function-call/issues"
  },
  "homepage": "https://github.com/azu/babel-plugin-strip-function-call",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-jsdoc-to-assert": "^4.0.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.18.0",
    "mocha": "^3.1.2",
    "power-assert": "^1.4.1"
  }
}
